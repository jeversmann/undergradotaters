procedure realloc(ptr, size)
{
  analyze MemoryLeak {
    if (HeapCount : memory_chunk is-exactly Unused) {
      memory_chunk <- Error
    }

    default {
      memory_chunk <- Error
    }
  }

  analyze Effects {
    memory_chunk <- Error
    variable <- Thing
  }
}
